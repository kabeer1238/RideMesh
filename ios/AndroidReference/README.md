# Android reference for vc25

The included codec documents the same Android-compatible extended RML1 packet now emitted by iOS vc25: display name followed by an optional phone-length/phone tail.

The current Android compatibility build should continue subscribing to:

`ridemesh/test/v3/<RIDE_CODE>/location`

The optional phone number is used only to populate the selected rider's Call/Message actions.
